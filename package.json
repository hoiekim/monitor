{
  "name": "monitor",
  "version": "1.0.0",
  "description": "Prod monitor — healthcheck, Docker events, log server, Discord alarms",
  "scripts": {
    "start": "bun src/monitor.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
